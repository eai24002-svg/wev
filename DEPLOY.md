# 🚀 تعليمات رفع الموقع على الاستضافة

## ✅ بعد البناء (npm run build)

تم إنشاء مجلد `dist` يحتوي على جميع الملفات الجاهزة للرفع.

## 📤 خطوات الرفع على Hostinger (أو أي استضافة)

### الطريقة 1: عبر File Manager في cPanel

1. **الدخول إلى cPanel:**
   - سجل الدخول إلى حسابك في Hostinger
   - افتح **File Manager**

2. **الانتقال إلى المجلد الصحيح:**
   - اذهب إلى مجلد `public_html` (أو `www` أو `htdocs` حسب الاستضافة)

3. **حذف الملفات القديمة (إن وجدت):**
   - احذف جميع الملفات الموجودة في `public_html`
   - **تحذير:** تأكد من عمل نسخة احتياطية أولاً

4. **رفع الملفات:**
   - افتح مجلد `dist` من جهازك
   - **ارفع جميع محتويات** مجلد `dist` إلى `public_html`
   - **مهم:** ارفع محتويات `dist` وليس المجلد نفسه

5. **الملفات المطلوبة:**
   ```
   public_html/
   ├── index.html          ← يجب أن يكون في الجذر
   ├── assets/             ← مجلد JavaScript و CSS
   ├── logo.svg            ← لوجو الموقع
   ├── logos/              ← مجلد اللوجوهات (150+ ملف)
   ├── placeholder.png     ← صورة placeholder
   └── og-placeholder.svg  ← صورة OpenGraph
   ```

### الطريقة 2: عبر FTP (FileZilla أو أي برنامج FTP)

1. **الحصول على بيانات FTP:**
   - من cPanel → **FTP Accounts**
   - أو استخدم بيانات cPanel الرئيسية

2. **الاتصال:**
   - Host: `ftp.yourdomain.com` أو IP الخاص بالاستضافة
   - Username: اسم المستخدم
   - Password: كلمة المرور
   - Port: 21

3. **رفع الملفات:**
   - في الجانب الأيسر: افتح مجلد `dist` من جهازك
   - في الجانب الأيمن: اذهب إلى `public_html`
   - اسحب جميع الملفات من `dist` إلى `public_html`

### الطريقة 3: عبر SSH (للمستخدمين المتقدمين)

```bash
# الاتصال بالخادم
ssh username@yourdomain.com

# الانتقال إلى المجلد
cd public_html

# رفع الملفات (من جهازك المحلي)
scp -r dist/* username@yourdomain.com:~/public_html/
```

## ✅ التحقق من الرفع

بعد الرفع، تحقق من:

1. ✅ وجود `index.html` في الجذر
2. ✅ وجود مجلد `assets` مع الملفات
3. ✅ وجود مجلد `logos` مع جميع اللوجوهات
4. ✅ وجود `logo.svg` في الجذر
5. ✅ فتح الموقع في المتصفح والتحقق من:
   - الصفحة الرئيسية تعمل
   - جميع الصور واللوجوهات تظهر
   - الروابط تعمل بشكل صحيح

## 🔧 حل المشاكل الشائعة

### المشكلة: الصور لا تظهر
- **الحل:** تأكد من رفع مجلد `logos` بالكامل
- تأكد من أن المسارات صحيحة (يجب أن تكون `/logos/filename.png`)

### المشكلة: الصفحات تعطي 404
- **الحل:** تأكد من وجود ملف `.htaccess` في الجذر (إن لم يكن موجوداً، أنشئه)

### المشكلة: الموقع لا يعمل
- **الحل:** 
  1. تأكد من رفع جميع الملفات من `dist`
  2. تأكد من وجود `index.html` في الجذر
  3. امسح الكاش من المتصفح (Ctrl+F5)

## 📝 ملف .htaccess (إن لم يكن موجوداً)

أنشئ ملف `.htaccess` في `public_html` مع المحتوى التالي:

```apache
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /
  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /index.html [L]
</IfModule>
```

هذا الملف يضمن أن Vue Router يعمل بشكل صحيح.

## 🎯 ملخص سريع

1. ✅ شغّل `npm run build`
2. ✅ افتح مجلد `dist`
3. ✅ ارفع **جميع محتويات** `dist` إلى `public_html`
4. ✅ تأكد من وجود `index.html` في الجذر
5. ✅ افتح الموقع في المتصفح

---

**ملاحظة:** الموقع يعمل كـ Front-End فقط، لا يحتاج إلى قاعدة بيانات أو Backend.

© 2025 AHMED SAADOON PRODUCTION STUDIO

